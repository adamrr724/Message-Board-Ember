<div class="container">
  <div class="">
    <h2>{{model.content}}</h2>
    <h6>Asked by: {{model.name}}</h6>
  </div>
  <div class="text__area">
    <img class="post__image" src="{{model.image}}" alt="" />
  </div>

    <div class="answers">
      <h2>Answers</h2>
      {{answer-tile  question=model}}
      {{new-answer addNewAnswer2="addNewAnswer3" question=model}}
    </div>
</div>
