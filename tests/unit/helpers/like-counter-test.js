import { likeCounter } from 'message-board/helpers/like-counter';
import { module, test } from 'qunit';

module('Unit | Helper | like counter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = likeCounter([42]);
  assert.ok(result);
});
