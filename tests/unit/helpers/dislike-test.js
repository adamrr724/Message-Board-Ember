import { dislike } from 'message-board/helpers/dislike';
import { module, test } from 'qunit';

module('Unit | Helper | dislike');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dislike([42]);
  assert.ok(result);
});
