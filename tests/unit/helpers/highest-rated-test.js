import { highestRated } from 'message-board/helpers/highest-rated';
import { module, test } from 'qunit';

module('Unit | Helper | highest rated');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = highestRated([42]);
  assert.ok(result);
});
