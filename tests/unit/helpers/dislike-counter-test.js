import { dislikeCounter } from 'message-board/helpers/dislike-counter';
import { module, test } from 'qunit';

module('Unit | Helper | dislike counter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dislikeCounter([42]);
  assert.ok(result);
});
